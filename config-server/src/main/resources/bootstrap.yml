spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/giblunziz/config
          clone-on-start: true
          basedir: /git/repo
          force-pull: true
          search-paths: '{application}'

